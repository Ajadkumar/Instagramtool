<!DOCTYPE html><html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="description" content="AI-based Image Compression Tool - Optimize your images for faster USA traffic delivery and SEO performance.">
  <meta name="keywords" content="Image Compression, AI Image Optimizer, SEO Tool, USA Traffic Optimization">
  <meta name="author" content="Your Name">
  <title>AI Image Compression Tool - USA Optimized</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      padding: 20px;
      background: #f9f9f9;
    }
    .container {
      max-width: 600px;
      margin: auto;
      background: #fff;
      padding: 20px;
      border-radius: 10px;
      box-shadow: 0 0 10px rgba(0,0,0,0.1);
    }
    h1 {
      text-align: center;
      font-size: 24px;
    }
    input[type="file"], input[type="range"] {
      display: block;
      margin: 15px 0;
    }
    .ad-section {
      margin: 20px 0;
      text-align: center;
    }
    .compressed-result img {
      max-width: 100%;
      margin-top: 20px;
    }
    button {
      background: #007bff;
      color: #fff;
      padding: 10px 20px;
      border: none;
      border-radius: 5px;
      cursor: pointer;
    }
    button:hover {
      background: #0056b3;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>🧠 AI Image Compression Tool</h1>
    <div class="ad-section">
      <!-- Adsttra Ad Unit 1 -->
    <script type="text/javascript">
	atOptions = {
		'key' : 'bf6d6594e62549bf0ca1abd78ca28413',
		'format' : 'iframe',
		'height' : 600,
		'width' : 160,
		'params' : {}
	};
</script>
<script type="text/javascript" src="//denialtransportationfair.com/bf6d6594e62549bf0ca1abd78ca28413/invoke.js"></script>
    </div><label for="upload">Select Image:</label>
<input type="file" id="upload" accept="image/*">

<label for="quality">Compression Level: <span id="quality-value">50</span>%</label>
<input type="range" id="quality" min="10" max="90" value="50">

<button onclick="compressImage()">Compress Image</button>

<div class="compressed-result" id="result"></div>

<div class="ad-section">
  <!-- Adsttra Ad Unit 2 -->
  <script type="text/javascript">
	atOptions = {
		'key' : 'bf6d6594e62549bf0ca1abd78ca28413',
		'format' : 'iframe',
		'height' : 600,
		'width' : 160,
		'params' : {}
	};
</script>
<script type="text/javascript" src="//denialtransportationfair.com/bf6d6594e62549bf0ca1abd78ca28413/invoke.js"></script>
</div>

  </div>  <script>
    document.getElementById('quality').addEventListener('input', function() {
      document.getElementById('quality-value').textContent = this.value;
    });

    function compressImage() {
      const file = document.getElementById('upload').files[0];
      const quality = document.getElementById('quality').value;
      const resultDiv = document.getElementById('result');

      if (!file) {
        alert("Please select an image.");
        return;
      }

      const reader = new FileReader();
      reader.onload = function (e) {
        const img = new Image();
        img.src = e.target.result;
        img.onload = function () {
          const canvas = document.createElement('canvas');
          const ctx = canvas.getContext('2d');
          canvas.width = img.width;
          canvas.height = img.height;
          ctx.drawImage(img, 0, 0);

          const compressedData = canvas.toDataURL('image/jpeg', quality / 100);
          resultDiv.innerHTML = `<h3>Compressed Image:</h3><img src="${compressedData}">`;
        };
      };
      reader.readAsDataURL(file);
    }
  </script></body>
</html>
